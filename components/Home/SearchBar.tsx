import React from 'react';
import styled from '@emotion/styled';

function SearchBar() {
  return (
    <Styled.Root>
      <input></input>
    </Styled.Root>
  );
}

export default SearchBar;

const Styled = {
  Root: styled.div`
};
